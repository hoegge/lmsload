[project]
name = "lmsload"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyyaml",
    "windows-curses; sys_platform == 'win32'",
]
